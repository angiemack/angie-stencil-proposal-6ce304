/**
 * Paths to pre-render at build time.
 * Add public-facing pages here (e.g. "/", "/about", "/pricing").
 * Authenticated/dynamic pages use SSR automatically — do not add them here.
 */
const prerender: string[] = ["/"];

export default prerender;
